# Requirements file for the CloudShip application. This file lists the Python packages required to run the application. The only package specified is Flask, which is a lightweight web framework for building web applications in Python. You can install the required packages using the command `pip install -r requirements.txt`.

flask