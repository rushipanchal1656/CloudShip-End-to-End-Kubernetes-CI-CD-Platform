apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloudship-ingress
  namespace: cloudship
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloudship-service
            port:
              number: 80
